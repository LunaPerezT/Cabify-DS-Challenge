# Cabify Data Science Challenge
#
# Results in this repository were produced with Python 3.12.3 and the exact
# versions listed in the right-hand comments. The floors below are the minimum
# versions the code actually needs, so a slightly older stack will also work.

numpy>=2.0            # 2.4.4   np.trapezoid needs numpy 2.x
pandas>=2.0           # 3.0.2
scipy>=1.10           # 1.17.1
matplotlib>=3.7       # 3.10.8
scikit-learn>=1.3     # 1.8.0
joblib>=1.3           # 1.5.3

# Part 2 only, used in the model search. Everything else runs without them.
lightgbm>=4.0         # 4.7.0
xgboost>=2.0          # 3.4.0

# To open and run the notebooks.
jupyterlab>=4.0
